-- migration_035: убрать неиспользуемую колонку profiles.ai_profile_enc
--
-- public.profiles.ai_profile_enc — production-only артефакт: text, nullable,
-- без default. В репозитории её определения нет и никогда не было (проверено
-- по всей истории git), в коде приложения она не упоминается.
--
-- Аудит перед удалением (CORE-SCHEMA-HYGIENE-D), только агрегаты:
--   профилей всего — 3, из них с непустым ai_profile_enc — 0;
--   зависимости по всем классам — ноль: индексы, ограничения, defaults,
--   триггеры, функции, представления, RLS-политики, rules.
-- Записать в неё ничего нельзя: колонка не входит в column-level allow-list
-- UPDATE (migration_020), поэтому ни один клиентский путь её не заполняет.
-- CASCADE не нужен и намеренно не используется.
--
-- Причина: канонический E2E baseline должен отражать целевую архитектуру,
-- а не пустую колонку неизвестного происхождения. Если функциональность
-- AI-профиля понадобится, схему следует вводить осознанно — отдельной
-- миграцией с явным дизайном хранения и доступа, а не наследовать этот
-- артефакт.
--
-- Применение: вручную через Supabase Studio SQL Editor, как вся история
-- миграций этого проекта.

begin;

alter table public.profiles
  drop column if exists ai_profile_enc;

commit;
