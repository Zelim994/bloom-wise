-- migration_034: убрать диагностическую функцию bw_plpgsql_test
--
-- public.bw_plpgsql_test() — production-only артефакт: в репозитории его
-- определения нет и никогда не было (проверено по всей истории git), в коде
-- приложения он не используется. Тело возвращает константу
-- jsonb_build_object('ok', true): не читает и не меняет никаких данных,
-- не вызывает функций приложения, не обращается к auth/организации.
--
-- Трассировка зависимостей (CORE-SCHEMA-HYGIENE-A) дала ноль по всем классам:
-- pg_depend, триггеры, событийные триггеры, функции/процедуры, представления,
-- материализованные представления, RLS-политики, defaults, constraints, rules.
-- Поэтому CASCADE не нужен и намеренно не используется.
--
-- Причина удаления: канонический E2E baseline должен отражать целевую
-- архитектуру, а не замороженный одноразовый smoke-test. Отдельно снимать
-- привилегии не требуется — объект убирается целиком.
--
-- Применение: вручную через Supabase Studio SQL Editor, как вся история
-- миграций этого проекта.

begin;

drop function if exists public.bw_plpgsql_test();

commit;
